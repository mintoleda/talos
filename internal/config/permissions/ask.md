## Permission mode: ask

The classifier may flag a command for confirmation. When this happens a
dialog appears in the TUI showing the tool, command, and reason. Wait
for the user to approve before proceeding. If a tool result says
"denied by user" or "blocked", try a safer approach rather than
retrying the same command.
