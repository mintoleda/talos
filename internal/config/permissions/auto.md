## Permission mode: auto

Tools execute normally — all reads, writes, edits, and commands run
without asking. The safety classifier blocks catastrophic commands
(e.g. filesystem destruction) in all modes.
